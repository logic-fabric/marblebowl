import { PaletteComponent } from "./palette.js";

const neutralPalette = new PaletteComponent(["neutral"], 3);
neutralPalette.render();

const magentaPalette = new PaletteComponent(["magenta"], 3);
magentaPalette.render();
const yellowPalette = new PaletteComponent(["yellow"], 3);
yellowPalette.render();
const cyanPalette = new PaletteComponent(["cyan"], 3);
cyanPalette.render();

const redPalette = new PaletteComponent(["red"], 3);
redPalette.render();
const greenPalette = new PaletteComponent(["green"], 3);
greenPalette.render();
const bluePalette = new PaletteComponent(["blue"], 3);
bluePalette.render();
